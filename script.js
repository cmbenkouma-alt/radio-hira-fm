const menuToggle=document.querySelector('.menu-toggle');const nav=document.querySelector('.nav-links');const playBtn=document.querySelector('#playBtn');const player=document.querySelector('#radioPlayer');const status=document.querySelector('#playerStatus');document.querySelector('#year').textContent=new Date().getFullYear();menuToggle?.addEventListener('click',()=>nav.classList.toggle('open'));nav?.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));
// Remplacez cette valeur par l'URL réelle du flux audio Radio Hira FM.
const RADIO_STREAM_URL='';
playBtn?.addEventListener('click',async()=>{if(!RADIO_STREAM_URL){status.textContent='Flux radio à connecter — URL à renseigner dans script.js';return}if(player.paused){player.src=RADIO_STREAM_URL;try{await player.play();playBtn.textContent='❚❚';status.textContent='Lecture en direct';}catch(e){status.textContent='Impossible de lancer le flux';}}else{player.pause();playBtn.textContent='▶';status.textContent='Lecture en pause';}});
